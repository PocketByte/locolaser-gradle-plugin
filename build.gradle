apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.3.11'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

ext {
    plugin_version = '1.0.3'
    plugin_group = 'ru.pocketbyte.locolaser'
}